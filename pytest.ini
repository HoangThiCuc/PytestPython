[pytest]
# This tells pytest-playwright to automatically capture artifacts ONLY when a test fails
addopts =
    --tracing retain-on-failure
    --screenshot only-on-failure
    --video retain-on-failure
    --output test-results/
    